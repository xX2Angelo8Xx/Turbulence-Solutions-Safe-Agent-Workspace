# DOC-049 Dev Log — Update architecture.md for v3.3.x

## Status
In Progress

## Assigned To
Developer Agent

## Date
2026-04-01

## Objective
Update `docs/architecture.md` to reflect all changes introduced between v3.2.6 and v3.3.6:
- AgentDocs system (`templates/agent-workbench/AgentDocs/`)
- Agent redesign (`.github/agents/` with redesigned agent definitions and skills)
- Workspace prefix rename (`TS-SAE-` → `SAE-`)
- Template rename (`safe-agent-workspace` → `agent-workbench`, plus new `certification-pipeline` stub)
- Version history entries for v3.3.0–v3.3.6
- Security policy update (configurable denial counter)

## Implementation Summary

### Changes Made to `docs/architecture.md`

1. **Added "Template System" section** describing `agent-workbench` and `certification-pipeline` templates.
2. **Added "Agent System" section** describing the redesigned agents in `.github/agents/` and the AgentDocs system.
3. **Added "Workspace Prefix" section** documenting the `SAE-` prefix (replacing old `TS-SAE-` prefix).
4. **Added "Version History" section** with entries for v3.3.0 through v3.3.6.
5. **Updated Security Policy section** to reference the configurable denial counter (SAF-036/SAF-037).

### No stale references found
- No `TS-SAE-` or `safe-agent-workspace` references were present in any prose section.
- The repository structure tree is auto-generated by `scripts/update_architecture.py` and was not manually edited.

## Files Changed
- `docs/architecture.md`
- `docs/workpackages/workpackages.csv`

## Tests Written
- `tests/DOC-049/test_doc049_architecture.py`
  - Verifies `v3.3.6` is referenced
  - Verifies no `TS-SAE-` occurrences in prose sections
  - Verifies `agent-workbench` is referenced
  - Verifies `AgentDocs` is referenced
  - Verifies `SAE-` prefix is referenced
  - Verifies Version History section exists

## Test Results
All tests passed. See `docs/test-results/test-results.csv` for log entry.

## Known Limitations
None.

---

## Iteration 2 — 2026-04-01

### Tester Findings (from test-report.md)
Three edge-case tests added by the Tester failed:
1. Wrong AgentDocs path (`templates/agent-workbench/AgentDocs/` → should be `templates/agent-workbench/Project/AgentDocs/`)
2. Non-existent files `TOOL-MATRIX.md` and `QUICKREF.md` listed in AgentDocs table
3. Non-existent skills directory `.github/agents/skills/` claimed in Agent System section

### Fixes Applied

1. **AgentDocs path**: Updated `docs/architecture.md` line referencing AgentDocs to use correct path `templates/agent-workbench/Project/AgentDocs/`.

2. **AgentDocs file table**: Removed `TOOL-MATRIX.md` and `QUICKREF.md` entries. Replaced with actual files found at `templates/agent-workbench/Project/AgentDocs/`: `AGENT-RULES.md`, `architecture.md`, `decisions.md`, `open-questions.md`, `plan.md`, `progress.md`, `research-log.md`.

3. **Skills directory**: Removed incorrect sentence "Reusable skill definitions are stored in `.github/agents/skills/` and are referenced from agent frontmatter." (that directory does not exist in the source repo; template skills live at `templates/agent-workbench/.github/skills/`).

4. **Test fix**: Updated `test_agentdocs_path_is_accurate` in `tests/DOC-049/test_doc049_architecture.py` — the Tester's test had a broken assertion checking a hardcoded non-existent path (`agent-workbench/AgentDocs`). Fixed to check that (a) `actual_path` exists on disk and (b) architecture.md contains the correct path reference.

### Test Results
- 15 passed, 0 failed (`tests/DOC-049/`)
- TST-2414 logged
