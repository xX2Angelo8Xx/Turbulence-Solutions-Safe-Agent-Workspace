# FIX-011 Test Report

## Summary

| Item | Detail |
|------|--------|
| WP | FIX-011 — Fix CI Spec File and Drop Intel Mac Build |
| Verdict | **PASS** |
| Tester | Tester Agent |
| Date | 2026-03-14 |
| Full suite result | 1854 passed, 29 skipped, 0 failed |

---

## 1. Code Review

### `.gitignore`
- **PASS**: `*.spec` exclusion rule is present ✅
- **PASS**: `!launcher.spec` negation appears immediately after `*.spec` (correct git ordering) ✅
- **PASS**: Negation line is exactly `!launcher.spec` with no extra whitespace ✅
- **PASS**: The negation is the specific minimal fix — general `*.spec` rule preserved ✅

### `.github/workflows/release.yml`
- **PASS**: `macos-intel-build` job is completely absent from the `jobs` dict ✅
- **PASS**: No textual reference to `macos-intel-build` anywhere in the raw YAML (including comments) ✅
- **PASS**: No reference to `macos-13` runner (Intel Mac runner fully removed) ✅
- **PASS**: `release` job `needs` is exactly `[windows-build, macos-arm-build, linux-build]` ✅
- **PASS**: `macos-arm-build` runner unchanged at `macos-14` ✅
- **PASS**: Workflow still has 4 total jobs (3 build + 1 release) ✅

### `launcher.spec`
- **PASS**: File exists at repository root ✅
- **PASS**: File is tracked by `git ls-files` (confirmed via `git add -f`) ✅
- **PASS**: `git check-ignore` does not flag it as ignored (negation is effective) ✅
- **PASS**: Valid Python syntax (`ast.parse` succeeds) ✅
- **PASS**: Contains all required PyInstaller blocks: `Analysis`, `EXE`, `COLLECT` ✅

### Updated Test Files
All files modified by the developer were reviewed:

| File | Review Result |
|------|---------------|
| `tests/INS-012/test_ins012_gitignore.py` | ✅ `test_gitignore_git_recognises_spec` correctly updated to verify `launcher.spec` is **tracked** (not ignored) |
| `tests/INS-013/test_ins013_ci_workflow.py` | ✅ `EXPECTED_JOBS` updated to 4 jobs; `test_macos_intel_runs_on` now asserts absence; `test_release_needs_all_builds` checks only 3 build jobs |
| `tests/INS-013/test_ins013_tester_edge_cases.py` | ✅ `EXPECTED_BUILD_JOBS` updated to 3 jobs; `test_no_duplicate_job_names` expects 4 total; `test_release_needs_count_is_exactly_4` asserts `len==3` |
| `tests/INS-015/test_ins015_macos_build_jobs.py` | ✅ `intel_job` fixture skips if job absent; `test_macos_intel_job_exists` asserts absence |
| `tests/INS-015/test_ins015_edge_cases.py` | ✅ `intel_job` fixture skips if job absent; intel-dependent tests auto-skipped; ARM tests unaffected |
| `tests/INS-017/test_ins017_release_job.py` | ✅ `EXPECTED_NEEDS` updated to 3 build jobs; `test_release_needs_macos_intel_build` asserts non-membership |
| `tests/INS-017/test_ins017_edge_cases.py` | ✅ `test_release_needs_exactly_four_jobs` asserts `len==3` |

**Note**: The dev-log does not mention INS-013 or INS-017 as changed files, but both were updated correctly. This is a documentation gap in the dev-log (minor finding, does not block PASS).

---

## 2. Test Execution

### Pre-edge-case baseline
- Developer test suite: **9 tests** in `test_fix011_spec_tracking.py`, all pass.
- Note: dev-log reports 5 tests, but the file contains 9 (dev-log under-counted). All 9 are valid and pass.
- Full regression run: **1845 passed, 29 skipped, 0 failed** (matches developer report).

### Tester edge-case tests (`tests/FIX-011/test_fix011_edge_cases.py`)
9 tests added covering:
1. `launcher.spec` has valid Python syntax
2. `launcher.spec` contains `Analysis()` block
3. `launcher.spec` contains `EXE()` block
4. `launcher.spec` contains `COLLECT()` block
5. Raw `release.yml` text has no `macos-intel-build` reference (including comments)
6. Raw `release.yml` text has no `macos-13` runner reference
7. `macos-arm-build` runner is still `macos-14` (regression guard)
8. `!launcher.spec` negation line has exact correct syntax in `.gitignore`
9. Other `*.spec` files (e.g. `autogenerated.spec`) are still ignored by git

All 9 edge-case tests **PASS**.

### Post-edge-case full regression
- **1854 passed, 29 skipped, 0 failed**
- The 29 skipped are Intel Mac tests in INS-015 (skip when `macos-intel-build` job is absent — by design).
- No regressions introduced.

---

## 3. Security Analysis

| Vector | Assessment |
|--------|-----------|
| `.gitignore` negation ordering | ✅ `!launcher.spec` appears **after** `*.spec` — git processes rules in order; negation after exclusion is the correct and only valid form |
| Negation too broad | ✅ Negation is specific to `launcher.spec` — other auto-generated spec files remain excluded |
| Stray `macos-intel-build` in comments | ✅ Raw text check confirms no residual references |
| ARM runner accidentally changed | ✅ Confirmed `macos-14` unchanged |
| No shell-injection risk | N/A — file configuration changes only |

---

## 4. Bugs Logged

None. No new bugs found during this review.

---

## 5. Verdict

**PASS** — All acceptance criteria met. Implementation is correct and minimal. All tests pass. No regressions. WP status set to `Done`.
